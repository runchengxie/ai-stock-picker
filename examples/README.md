# 示例输入

- `us_candidates.json`：通用候选池 v1，美股示例
- `generic_cn_candidates.json`：通用候选池 v1，A 股示例
- `cn_candidates.json`：热题材候选池 v1，A 股上游契约示例

这些文件都是输入示例，不是模型生成结果，因此保留在 `examples/`。

市场来自 manifest。任一示例都可以搭配 DeepSeek、Gemini 或 OpenAI-compatible provider 执行 dry-run。
